syntax = "proto3";

package wfa.measurement.common;

option java_package = "org.wfanet.measurement.common";
option java_multiple_files = true;

service FakeService {
  rpc Fake(stream FakeRequest) returns (FakeResponse) {}
}

message FakeRequest {
  int32 number = 1;
}

message FakeResponse {
  // Deliberately empty
}